<h2>Shouts</h2>

<%= form_for @text_subject do |form| %>
  <%= form.text_field :body %>
<%= form.submit "Shout!" %>
<% end %>

<h4>People I do not follow</h4>
<ul>
  <% @users_i_do_not_follow.each do |user| %>
    <li>
    <%= user.email %>
    <%- button_to "Follow", [user, :following_relationship] %>
    </li>
  <% end %>
</ul>

<h4>People I follow</h4>
<ul>
  <% @users_i_follow.each do |user| %>
    <li>
    <%= user.email %>
    </li>
  <% end %>
</ul>

  <h4>Your Feed</h4>
  <ul>
    <%= render @timeline %>
  </ul>

